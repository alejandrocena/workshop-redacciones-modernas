import {Entity,Sentiment,Category} from './backend';

export const doProcess = async (post,onMessage=()=>{}) => {
  /*
  onMessage('Loading General Title Sentiment...');
  const title_sentiment = await Entity.sentiment(post.title);
  
  onMessage('Loading Title Entities And Sentiments...');
  const title_entities = await Entity.extract(post.title);
  
  console.log(title_sentiment,title_entities);
  */
  
  // title
    // sentimiento
    // entidades
  
  // Dropline
    // Tags entidades c/sentimiento
    // categorizacion
  
  // Imagenes
  
};